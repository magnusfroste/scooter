import React from 'react';
import BMap from '../BMap';
import PageContent from '../PageContent';

const Home = () => {
  return (
    <PageContent>
        <BMap />
    </PageContent>
  );
};

export default Home;
